/-
================================================================================
CompletingSquare.lean
Chapter 0.98.2 · 配方法：让机器逐字检查一次「A 类恒等变换」

文章里说：A 类动作（恒等变换）的验收标准是 `simplify(左 - 右) == 0`。
SymPy 能验，但它只是「算了一遍没发现问题」。
Lean 更狠一点：它要求你交出一个**证明对象**，编译通过 = 对任意 x、p、q 都成立。

    x² + px + q  与  (x + p/2)² - p²/4 + q  是同一个东西

因为整数里没有 1/2，这里用去分母的等价写法（这本身就是一次恒等变换：
「两边同乘 4」—— 工具箱 #1）：

    4(x² + px + q) = (2x + p)² - (p² - 4q)

配方法的真正目的不是记住套路，而是：
    **把未知数 x 从「出现在两个地方」逼成「只出现在一个地方」。**
左边 x 出现两次，右边只出现一次 —— 剩下的就是纯机械开方。

历史：巴比伦泥板 BM 13901（前 1800）用「补一个正方形」的几何做法；
花拉子米 820 年用一整段大白话描述同一件事；韦达 1591 之后才写得出符号。

运行：  lean ch00_98_deriving_formulas/CompletingSquare.lean
        （无输出 = 全部证明通过；只用 Lean 4 core，不需要 mathlib）
================================================================================
-/

/-- 配方恒等式（去分母版）。`grind` 内置的 ring 求解器负责验证。 -/
theorem complete_square (x p q : Int) :
    4 * (x ^ 2 + p * x + q) = (2 * x + p) ^ 2 - (p ^ 2 - 4 * q) := by
  grind

/-- 于是「解方程」这件事被翻译成了「开方」：
    x² + px + q = 0  ⟺  (2x + p)² = p² - 4q
    注意这是一个 **⟺**（等价），不是单向推出 —— 恒等变换是可逆的。 -/
theorem root_iff (x p q : Int) :
    x ^ 2 + p * x + q = 0 ↔ (2 * x + p) ^ 2 = p ^ 2 - 4 * q := by
  grind

/-- 花拉子米 820 年的原题：x² + 10x = 39。
    他的大白话：「取根数目之半，得五；自乘之，得二十五；加于三十九，得六十四；
    取其方根，得八；减去根数目之半，得三。」
    翻译成配方式：(x+5)² = 64。 -/
theorem al_khwarizmi (x : Int) : x ^ 2 + 10 * x = 39 ↔ (x + 5) ^ 2 = 64 := by
  grind

/-- 他只报了正根 3（九世纪的人不承认负数解）。
    但 (x+5)² = 64 有两个解：x = 3 和 x = -13。
    **符号代数比大白话多告诉了你一半的答案** —— 这就是好记号的价值。 -/
theorem al_khwarizmi_both_roots (x : Int) :
    x ^ 2 + 10 * x = 39 ↔ x = 3 ∨ x = -13 := by
  constructor
  · intro h
    have : (x - 3) * (x + 13) = 0 := by grind
    rcases Int.mul_eq_zero.mp this with h1 | h2
    · left;  grind
    · right; grind
  · intro h; rcases h with h | h <;> subst h <;> decide

/-- 一般二次式的配方：对任意首项系数 a ≠ 0 也成立（同样去分母）。
    这就是求根公式 x = (-b ± √(b²-4ac)) / 2a 的全部内容 ——
    判别式 b² - 4ac 不是背下来的，是配方后**剩下的那一坨**。 -/
theorem complete_square_general (a b c x : Int) :
    4 * a * (a * x ^ 2 + b * x + c) = (2 * a * x + b) ^ 2 - (b ^ 2 - 4 * a * c) := by
  grind

/-
--------------------------------------------------------------------------------
读这个文件要读出的东西：

  · 每一条 theorem 都是一次 **A 类动作**：等号两边信息量完全相同。
  · `grind` 一行就能过，是因为这些全是纯代数重写，机器可以穷尽验证。
  · 反过来，凡是 `grind` 过不去的，多半不是恒等变换 —— 而是你偷偷
    塞进了一条新假设（B 类动作），却没写出来。见 CancelFallacy.lean。

  一句话：**恒等变换只有换衣服的权利，没有换人的权利。**
--------------------------------------------------------------------------------
-/
