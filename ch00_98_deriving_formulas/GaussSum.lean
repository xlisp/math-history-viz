/-
================================================================================
GaussSum.lean
Chapter 0.98.2 工具箱 #11 · 归纳法：把无穷降为有限

1787 年，9 岁的高斯被罚算 1+2+…+100，几秒钟交卷 5050。
他看到的是**配对的对称性**（Chapter 0 的母题）：

    1 + 2 + … + 99 + 100
  100 + 99 + … +  2 +  1
  ─────────────────────────
  101 + 101 + … + 101 + 101   = 100 × 101，除以 2 得 5050

配对是**发现**这个公式的方法；而**证明**它对任意 n 成立，用的是另一个动作：
数学归纳法（帕斯卡 1654 首次明确表述，欧几里得已有实质用法）。

    归纳法 = 用自然数的良序原理，把「无穷多个命题」降为「两个命题」：
      基础：n = 0 时成立
      递推：n 成立 ⇒ n+1 成立

这是工具箱里唯一能一次性征服无穷的动作。

为避免自然数减法/除法的坑，这里证的是**去分母版本**（同样是一次恒等变换）：

    2 · (1 + 2 + … + n) = n(n+1)

运行：  lean ch00_98_deriving_formulas/GaussSum.lean
        （只用 Lean 4 core，不需要 mathlib）
================================================================================
-/

/-- 递归定义「前 n 项和」。这就是 ∑ 符号在代码里的全部内容（Chapter 0.6）：
    一个 for 循环，或者等价地，一个递归。 -/
def sumTo : Nat → Nat
  | 0 => 0
  | n + 1 => sumTo n + (n + 1)

/-- 高斯求和公式（去分母版）。证明只有两行 —— 因为归纳法把无穷降成了两种情况。 -/
theorem gauss_sum (n : Nat) : 2 * sumTo n = n * (n + 1) := by
  induction n with
  | zero => rfl                       -- 基础：0 = 0
  | succ k ih => simp [sumTo]; grind  -- 递推：用上 ih，剩下的是代数

/-- 高斯本人算的那道题。`decide` 直接把 101 个数加了一遍 ——
    机器不需要技巧，但**技巧让人类不需要机器**。 -/
theorem gauss_1787 : sumTo 100 = 5050 := by decide

/-- 顺带一个同款：前 n 个奇数之和 = n²。
    几何图像：把 1、3、5、7… 个点摆成 L 形，每次补一层，正好凑成正方形
    —— 毕达哥拉斯学派（前 500）用石子摆出来的「形数」。 -/
def sumOdd : Nat → Nat
  | 0 => 0
  | n + 1 => sumOdd n + (2 * n + 1)

theorem sum_of_odds (n : Nat) : sumOdd n = n * n := by
  induction n with
  | zero => rfl
  | succ k ih => simp [sumOdd, ih]; grind

/-- 再来一个：前 n 个平方数之和（阿基米德在算抛物线面积时就需要它）。
    去分母版：6 · (1² + 2² + … + n²) = n(n+1)(2n+1) -/
def sumSq : Nat → Nat
  | 0 => 0
  | n + 1 => sumSq n + (n + 1) * (n + 1)

theorem sum_of_squares (n : Nat) : 6 * sumSq n = n * (n + 1) * (2 * n + 1) := by
  induction n with
  | zero => rfl
  | succ k ih => simp [sumSq]; grind

/-- 数值抽查：公式和递归定义必须给出同一个数（这就是「对拍」，只不过是形式化版的）。 -/
example : 2 * sumTo 7 = 7 * 8 := by decide
example : sumOdd 12 = 144 := by decide
example : 6 * sumSq 10 = 10 * 11 * 21 := by decide

/-
--------------------------------------------------------------------------------
读这个文件要读出的东西：

  · **发现**和**证明**是两个不同的动作，用的工具也不同。
    配对（对称性，工具箱 #9）负责让你「看出」答案；
    归纳（#11）负责让答案对所有 n 都站得住。

  · 三条公式的证明**长得一模一样**：`zero => rfl`，`succ => simp; grind`。
    这不是巧合 —— 它们是同一个母题（把递推关系闭式化）的三次投影。
    教辅书把它们列成三道独立的题，掩盖了这一点。

  · 去分母（2·、6·）是为了绕开自然数除法。这本身是工具箱 #1
    「两边同做一件事」的应用，也是一次纯 A 类恒等变换。
--------------------------------------------------------------------------------
-/
